# Context Map

## Contexts

- [Backend](./backend/CONTEXT.md): the generic API vocabulary (User, Role, Resource)

The frontend has no glossary yet; `frontend/CONTEXT.md` is created when its first term is resolved.

## Relationships

- **Frontend → Backend**: the frontend is a client of the backend REST API and uses the backend glossary's vocabulary.
